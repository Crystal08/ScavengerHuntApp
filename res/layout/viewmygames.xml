<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center_vertical"
    android:layout_margin="10dp" >

    <TextView
        android:id="@+id/header_myItemsList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:text="@string/header_myGamesList"
        android:textSize="18.0sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/manageItemsButton_done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:gravity="center"
        android:text="@string/label_back"
        android:textSize="16sp"
        android:textStyle="normal" />

    <ListView
        android:id="@+id/myGamesListView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/manageItemsButton_done"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/header_myItemsList"
        android:layout_marginRight="19dp" >

    </ListView>
    
</RelativeLayout>